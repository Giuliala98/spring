server:
  port: 8080
---

environment: production
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/es_hibernate
    username: root
    password: 14011998
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: exes_hibernate